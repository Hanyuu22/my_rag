# 面试高频问题回答手册

> 字节三面 2026-04-23，适用于 rag_project + getjob_agent 项目追问

---

## Q1：最有挑战的技术问题是什么，怎么解决的？

**答法原则：一个具体问题 + 定位过程 + 解决方案 + 结果**

---

### 推荐答法（挑 KG 冷启动问题，最有技术深度）

> "最有挑战的是知识图谱检索的冷启动问题。我们实现了一个实体-chunk 二部图，初始设计是每个 chunk 调一次 LLM 提取实体，2438 个 chunk 就是 2438 次 API 调用，串行下来需要 15-30 分钟，完全不可用。
>
> 我分析了问题的根本原因：LLM 调用慢不是因为模型能力，而是每次都有网络往返 + 云端排队开销，串行累积放大了这个问题。
>
> 解决方案分两步。第一步，用 jieba TF-IDF 替换 LLM 做实体提取，jieba 是纯内存计算，2438 个 chunk 处理只需 1.4 秒。但 jieba 默认词典不认识石化专业术语，会把'加氢裂化装置'拆成三个碎片，导致图的边匹配不上查询实体。
>
> 第二步，单独跑一个离线脚本，把 10 个 chunk 批量送给 LLM，提取领域专有术语写成 jieba userdict，2438 chunk 只需约 80 次 LLM 调用，5 分钟跑完。之后每次 KG 构建用 jieba 加载这个词典，速度从 30 分钟降到 2 秒。
>
> 效果上还有一个意外收获：jieba 是确定性的，同一个词在任何 chunk 里分词结果完全一致，图的节点天然统一，连通性比 LLM 提取的还要好，检索质量也提升了。"

---

### 备用答法（SSE 桥接后台线程问题，适合追问异步编程时用）

> "另一个挑战是 RAG 问答的流式输出。LangGraph 图是同步设计，跑在 `run_in_executor` 的后台线程里，但 FastAPI 的 SSE 需要在异步协程里 yield token。
>
> 问题是线程和协程不能直接通信——线程里的 `token_callback` 无法直接 await 协程里的 queue。
>
> 解决方案是用 `asyncio.Queue` 做桥接：后台线程调 `loop.call_soon_threadsafe(queue.put_nowait, token)` 把 token 扔进队列，协程里 `await queue.get()` 消费并 yield 给前端。用一个哨兵对象 `_SENTINEL` 标记线程执行完毕，协程收到哨兵后再 await graph 的 future 拿最终结果。"

---

## Q2：如果让你重做，会改什么？

**答法原则：说技术改进，体现成长，不否定现有方案，说清楚为什么现在没做**

---

> "有两个地方我会改。
>
> 第一是 LangGraph 的异步化。现在图是同步的，跑在线程池里，用 asyncio.Queue 桥接 SSE。这能工作，但本质是一个 workaround。LangGraph 支持 `ainvoke` 和 `astream_events` 原生异步接口，如果当时用异步设计，SSE 桥接会简洁很多，也能支持真正的并发——现在每个请求占一个线程，高并发下线程池会成为瓶颈。现在没改是因为图里有多处同步的 embedding 和 reranker 调用，全部换异步改动量较大，收益在单用户场景下不明显。
>
> 第二是知识图谱的增量更新。现在知识库新增文件后，KG 图缓存会失效，下次冷启动要重建整张图。更好的设计是只对新增的 chunk 提取实体并插入图，存量 chunk 不动。这需要在缓存里记录每个 chunk_id 的处理状态，改动不复杂，但当时优先解决了冷启动速度问题，增量更新还没做。"

---

## Q3：系统的瓶颈在哪？

**答法原则：分层回答，从用户感知到系统内部，说清楚数据支撑**

---

> "瓶颈主要在两个地方。
>
> 第一是 LLM 调用延迟。生成答案这一步无法优化，qwen-plus 大概 2-4 秒首 token，流式输出缓解了用户感知。真正的问题是检索链路里也有 LLM 调用：reranker 用的是 LLM 评分，query 改写也调一次 LLM，这两步加起来可能比生成本身还慢。优化方向是 reranker 换成本地 cross-encoder 模型，延迟从秒级降到百毫秒级。
>
> 第二是首次加载的冷启动。每个知识库第一次被请求时，需要从 Chroma 加载全量 chunk 构建 BM25 索引，2438 个 chunk 大概需要 8-12 秒。后续命中缓存就快了。根本解法是换成 Elasticsearch，入库时就建好倒排索引，每次查询直接走 ES，不需要在内存里重建。代码里已经有 `es_retriever.py`，ES 不在时自动降级到 BM25，架构上已经预留了这个扩展点。"

---

## Q4：为什么选这个方案而不是 XX？

### 4a：为什么用 Chroma 而不是 Faiss / Milvus / Qdrant？

> "Chroma 适合本地开发阶段——零配置，单文件持久化，Python 原生 API，启动不需要额外服务。Faiss 是纯内存库，没有持久化，适合离线批量检索。Milvus 和 Qdrant 是生产级向量数据库，支持分布式、水平扩展，但需要单独部署服务，对 demo 阶段成本偏高。我们 getjob_agent 用的就是 Qdrant，因为它有 REST API 和 Python SDK，接入比 Milvus 简单，适合单机部署。"

### 4b：为什么用 BM25 + 向量混合检索而不是纯向量？

> "纯向量检索对精确词匹配很弱。比如用户问'GB/T 50265 第 4.2 条'，这是一个标准编号，向量空间里找不到语义相近的内容。BM25 是关键词精确匹配，能直接命中。混合检索用 RRF（倒数排名融合）把两路结果合并，取长补短。实测在技术文档场景里，混合检索的召回率比纯向量高 15-20%。"

### 4c：为什么用 LangGraph 而不是直接写 Python？

> "核心原因是状态管理和可观测性。直接写 Python 的话，多 Agent 协调要手动传参，节点越多参数越乱，容易出现字段覆盖 bug。LangGraph 的 TypedDict + reducer 机制让每个节点只负责自己的字段，框架处理合并。另外 `astream_events` 能监听单个 LLM token，这是实现流式输出的基础，直接写循环很难做到同样细粒度的事件流。"

### 4d：为什么用父子 chunk 而不是直接加大 chunk size？

> "加大 chunk size 是简单粗暴的方案，会引入两个问题：一是检索精度下降，chunk 越大向量越稀释，相关性分数的区分度变弱；二是送给 LLM 的 context 里噪音增加，大 chunk 里往往只有一部分和问题相关。父子 chunk 的思路是分离检索单元和生成单元——小 chunk（512字）用于精准检索，命中后替换成对应的大 chunk（整节，约 2000字）送给 LLM，检索精度和生成质量两者兼顾。"

---

## Q5：RAG 和 Fine-tuning 怎么选？

**（高频，必准备）**

> "这两者解决的问题不一样，不是竞争关系。
>
> Fine-tuning 改变模型的参数，让模型学会新的行为模式或风格，但不适合注入频繁更新的知识——每次知识更新都要重新训练，成本高。
>
> RAG 不改模型，把外部知识在推理时注入 context，适合知识库需要频繁更新的场景，比如企业文档、法规标准。缺点是受 context window 限制，不能注入太多内容，而且检索质量直接影响答案质量。
>
> 实际工程里常见的做法是组合：用 Fine-tuning 让模型掌握领域术语和输出格式，用 RAG 注入最新的具体知识。比如医疗问答，Fine-tuning 让模型懂医学语言风格，RAG 提供最新的药物说明书。"

---

## Q6：Reranker 是什么，为什么要用？

> "检索的第一步是召回，用向量相似度或 BM25 快速筛出 top-k（比如 10 个）候选文档，速度快但精度一般。Reranker 是第二步，把这 10 个候选文档逐一和原始 query 做 cross-attention 打分，精度高很多，但计算量大，不适合对全量文档做。
>
> 为什么要两步？向量检索是 query 和 document 分别编码再算相似度（双塔），没有交互信息。Reranker 是 query+document 拼接后一起编码（cross-encoder），能捕捉精细的语义匹配。实测 Reranker 之后答案命中率提升明显，代价是每次检索多 0.5-1 秒。"

---

## Q7：怎么评估 RAG 系统的质量？

> "我们用了 RAGAS 框架，从三个维度评估。
>
> 第一是**Faithfulness（忠实度）**：答案是否基于检索到的文档，有没有幻觉。RAGAS 把答案拆成若干陈述，逐一判断每条陈述是否能从 context 里推导出来。
>
> 第二是**Answer Relevancy（答案相关性）**：答案是否回答了问题。用 LLM 从答案反推出若干问题，看这些反推的问题和原始问题的向量相似度。
>
> 第三是**Context Recall（上下文召回率）**：检索到的文档是否包含了回答问题所需的全部信息，需要有 ground truth 答案才能计算。
>
> 实际工程中 Faithfulness 和 Answer Relevancy 是无监督的，不需要标注数据，适合快速迭代评估。Context Recall 需要人工标注，我们用来做关键节点的离线评测。"

---

## 快速记忆卡片

```
冷启动问题  → jieba + 离线词典脚本，30分钟 → 2秒
SSE桥接     → asyncio.Queue + call_soon_threadsafe + _SENTINEL
重做改什么  → LangGraph 异步化 + KG 增量更新
系统瓶颈    → LLM 延迟（reranker 换本地）+ 冷启动（换 ES）
Chroma      → 开发阶段零配置，生产换 Milvus/Qdrant
混合检索    → BM25 精确词 + 向量语义 + RRF 融合，召回率 +15-20%
父子 chunk  → 小 chunk 检索 + 大 chunk 生成，两者兼顾
RAG vs FT   → RAG 注入知识（可更新），FT 改行为风格（不可频繁）
Reranker    → cross-encoder 精排，命中率↑，延迟 +0.5-1s
RAGAS       → Faithfulness + Answer Relevancy（无监督）+ Context Recall（有监督）
```
